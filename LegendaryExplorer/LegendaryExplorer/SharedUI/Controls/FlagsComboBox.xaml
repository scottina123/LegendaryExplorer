<UserControl x:Class="LegendaryExplorer.SharedUI.Controls.FlagsComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="300"
             x:Name="FlagsComboBoxControl">
    <Grid>
        <ComboBox x:Name="PART_ComboBox"
                  IsEditable="True"
                  IsReadOnly="True"
                  Text="{Binding SelectedText, ElementName=FlagsComboBoxControl, Mode=OneWay}"
                  GotFocus="PART_ComboBox_GotFocus"
                  Loaded="PART_ComboBox_Loaded">
            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding Item}"
                              IsChecked="{Binding IsSelected, Mode=TwoWay}"
                              Checked="CheckBox_CheckedChanged"
                              Unchecked="CheckBox_CheckedChanged"
                              Padding="4,2"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
