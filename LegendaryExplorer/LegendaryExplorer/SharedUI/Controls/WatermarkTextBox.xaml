<UserControl x:Class="LegendaryExplorer.SharedUI.Controls.WatermarkTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="200"
             x:Name="WatermarkTextBoxControl"
             Focusable="False">
    <Grid>
        <TextBox x:Name="PART_TextBox" 
                 Text="{Binding Text, ElementName=WatermarkTextBoxControl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 VerticalContentAlignment="{Binding VerticalContentAlignment, ElementName=WatermarkTextBoxControl}"
                 IsReadOnly="{Binding IsReadOnly, ElementName=WatermarkTextBoxControl}"/>
        <TextBlock x:Name="PART_Watermark"
                   Text="{Binding Watermark, ElementName=WatermarkTextBoxControl}"
                   VerticalAlignment="Center"
                   Margin="6,0,0,0"
                   Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                   IsHitTestVisible="False">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Text, ElementName=PART_TextBox}" Value=""/>
                                <Condition Binding="{Binding IsFocused, ElementName=PART_TextBox}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>
