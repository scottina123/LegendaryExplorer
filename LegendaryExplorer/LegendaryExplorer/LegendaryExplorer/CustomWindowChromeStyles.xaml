<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- =====================================================
         CUSTOM WINDOW CHROME STYLES
         These provide a fully themed custom title bar that
         respects dark mode, replacing the native Windows chrome.
         ===================================================== -->
    
    <!-- Title bar button base style -->
    <Style x:Key="WindowChromeButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="30"/>
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
        <Setter Property="IsTabStop" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                          Focusable="False"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Close button style (red on hover) -->
    <Style x:Key="WindowChromeCloseButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource WindowChromeButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter x:Name="contentPresenter"
                                          Focusable="False"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="False"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFE81123"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#FFF1707A"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Maximize/Restore button path data -->
    <Geometry x:Key="MaximizeGeometry">M0,0 L10,0 L10,10 L0,10 Z</Geometry>
    <Geometry x:Key="RestoreGeometry">M2,0 L10,0 L10,8 L8,8 L8,2 L2,2 Z M0,2 L8,2 L8,10 L0,10 Z</Geometry>
    
    <!-- Custom Window Style with themed chrome -->
    <Style x:Key="CustomChromeWindowStyle" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome CaptionHeight="30"
                              CornerRadius="0"
                              GlassFrameThickness="0"
                              ResizeBorderThickness="4"
                              UseAeroCaptionButtons="False"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border x:Name="WindowBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Custom Title Bar -->
                            <Grid x:Name="TitleBar" Grid.Row="0" 
                                  Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Window Icon -->
                                <Image Grid.Column="0" 
                                       Source="{TemplateBinding Icon}"
                                       Width="16" Height="16"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center"
                                       WindowChrome.IsHitTestVisibleInChrome="True"/>
                                
                                <!-- Window Title -->
                                <TextBlock Grid.Column="1" 
                                           Text="{TemplateBinding Title}"
                                           Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"
                                           TextTrimming="CharacterEllipsis"
                                           FontSize="12"/>
                                
                                <!-- Window Buttons -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal">
                                    <!-- Minimize Button -->
                                    <Button x:Name="MinimizeButton"
                                            Style="{StaticResource WindowChromeButtonStyle}"
                                            Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                                            ToolTip="Minimize">
                                        <Path Data="M0,5 L10,5"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1"
                                              Width="10" Height="10"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                    </Button>
                                    
                                    <!-- Maximize/Restore Button -->
                                    <Button x:Name="MaximizeRestoreButton"
                                            Style="{StaticResource WindowChromeButtonStyle}"
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                                            ToolTip="Maximize">
                                        <Path x:Name="MaximizeRestorePath"
                                              Data="{StaticResource MaximizeGeometry}"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1"
                                              Fill="Transparent"
                                              Width="10" Height="10"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                    </Button>
                                    
                                    <!-- Close Button -->
                                    <Button x:Name="CloseButton"
                                            Style="{StaticResource WindowChromeCloseButtonStyle}"
                                            Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            ToolTip="Close">
                                        <Path Data="M0,0 L10,10 M0,10 L10,0"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1"
                                              Width="10" Height="10"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Window Content -->
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter/>
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- When maximized, switch to restore button -->
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeRestorePath" Property="Data" Value="{StaticResource RestoreGeometry}"/>
                            <Setter TargetName="MaximizeRestoreButton" Property="Command" Value="{x:Static SystemCommands.RestoreWindowCommand}"/>
                            <Setter TargetName="MaximizeRestoreButton" Property="ToolTip" Value="Restore"/>
                            <Setter TargetName="WindowBorder" Property="BorderThickness" Value="0"/>
                            <Setter TargetName="WindowBorder" Property="Padding" Value="7"/>
                        </Trigger>
                        <!-- Hide maximize button if ResizeMode doesn't allow it -->
                        <Trigger Property="ResizeMode" Value="NoResize">
                            <Setter TargetName="MinimizeButton" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="MaximizeRestoreButton" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="ResizeMode" Value="CanMinimize">
                            <Setter TargetName="MaximizeRestoreButton" Property="IsEnabled" Value="False"/>
                        </Trigger>
                        <!-- Inactive window styling -->
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="TitleBar" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            <Setter TargetName="WindowBorder" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.InactiveBorderBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>
