<Window x:Class="LegendaryExplorer.UserControls.PackageEditorControls.ExperimentsBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Package Editor Experiments"
        Width="920"
        Height="640"
        MinWidth="700"
        MinHeight="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="Experiments are unsupported developer tools and may break packages, games, or toolset functionality."
                       TextWrapping="Wrap"
                       FontWeight="SemiBold"/>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox x:Name="SearchTextBox"
                     Grid.Column="1"
                     Padding="6,4"
                     TextChanged="SearchTextBox_TextChanged"
                     ToolTip="Search every experiment by name, category, or description."/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <TextBlock DockPanel.Dock="Top" Text="Categories" FontWeight="Bold" Margin="4,0,4,6"/>
                <ListBox x:Name="CategoriesListBox"
                         SelectionChanged="CategoriesListBox_SelectionChanged"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
            </DockPanel>

            <DockPanel Grid.Column="2">
                <TextBlock x:Name="ResultsHeaderTextBlock"
                           DockPanel.Dock="Top"
                           FontWeight="Bold"
                           Margin="4,0,4,6"/>
                <ListBox x:Name="ExperimentsListBox"
                         MouseDoubleClick="ExperimentsListBox_MouseDoubleClick"
                         KeyDown="ExperimentsListBox_KeyDown"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="IsEnabled" Value="{Binding IsEnabled}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="6"/>
                            <Setter Property="ToolTip" Value="{Binding Description}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Category}"
                                           Margin="0,2,0,0"
                                           Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                                <TextBlock Text="{Binding Description}"
                                           Margin="0,3,0,0"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="ResultCountTextBlock" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Run" MinWidth="80" Padding="12,4" Margin="0,0,8,0" Click="RunButton_Click" IsDefault="True"/>
                <Button Content="Close" MinWidth="80" Padding="12,4" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
