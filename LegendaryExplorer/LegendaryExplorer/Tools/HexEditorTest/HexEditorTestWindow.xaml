<Window x:Class="LegendaryExplorer.Tools.HexEditorTest.HexEditorTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LegendaryExplorer.Tools.HexEditorTest"
        xmlns:hexEditor="clr-namespace:LegendaryExplorer.UserControls.SharedToolControls.HexEditor"
        mc:Ignorable="d"
        Title="Hex Editor Test" 
        Height="600" 
        Width="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Background="{DynamicResource ToolBarBrush}">
            <Button Content="Open File" Click="OpenFile_Click" Padding="5,2"/>
            <Separator/>
            <CheckBox Content="Line Info" IsChecked="{Binding ElementName=HexEditor, Path=LineInfoVisible}" VerticalAlignment="Center" Margin="5,0"/>
            <CheckBox Content="Column Info" IsChecked="{Binding ElementName=HexEditor, Path=ColumnInfoVisible}" VerticalAlignment="Center" Margin="5,0"/>
            <CheckBox Content="String View" IsChecked="{Binding ElementName=HexEditor, Path=StringViewVisible}" VerticalAlignment="Center" Margin="5,0"/>
            <CheckBox Content="Group Separator" IsChecked="{Binding ElementName=HexEditor, Path=GroupSeparatorVisible}" VerticalAlignment="Center" Margin="5,0"/>
            <CheckBox Content="Read Only" IsChecked="{Binding ElementName=HexEditor, Path=ReadOnly}" VerticalAlignment="Center" Margin="5,0"/>
            <Separator/>
            <Button Content="Test Highlight" Click="TestHighlight_Click" Padding="5,2"/>
            <Button Content="Clear Highlights" Click="ClearHighlights_Click" Padding="5,2"/>
        </ToolBar>

        <!-- File Info -->
        <Border Grid.Row="1" 
                Background="{DynamicResource InfoBackgroundBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="0,1"
                Padding="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="File: " FontWeight="Bold" Margin="0,0,5,0"/>
                <TextBlock Grid.Column="1" x:Name="FilePathTextBlock" Text="No file loaded" TextTrimming="CharacterEllipsis"/>
                <TextBlock Grid.Column="2" x:Name="FileSizeTextBlock" Text="" Margin="10,0,0,0"/>
                <TextBlock Grid.Column="3" x:Name="BytesPerLineTextBlock" Text="" Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <!-- Hex Editor -->
        <Border Grid.Row="2" 
                BorderBrush="{DynamicResource BorderBrush}" 
                BorderThickness="1"
                Margin="5">
            <hexEditor:HexEditorControl x:Name="HexEditor"
                                       LineInfoVisible="True"
                                       ColumnInfoVisible="True"
                                       StringViewVisible="True"
                                       VScrollBarVisible="True"
                                       GroupSeparatorVisible="False"
                                       ReadOnly="False"
                                       FontFamily="Consolas"
                                       FontSize="12"/>
        </Border>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3" Background="{DynamicResource StatusBarBrush}">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" Text="Ready"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="SelectionTextBlock" Text="Selection: None"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="PositionTextBlock" Text="Position: 0x0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
