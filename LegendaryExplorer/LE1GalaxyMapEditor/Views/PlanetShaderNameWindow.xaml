<Window x:Class="LE1GalaxyMapEditor.Views.PlanetShaderNameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Planet Shader name"
        Width="520"
        Height="285"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="CHOOSE UNIQUE SHADER NAME"
                       Foreground="{DynamicResource AccentBrush}"
                       FontSize="11"
                       FontWeight="Bold"/>
            <TextBlock x:Name="ExplanationText"
                       Foreground="{DynamicResource MutedTextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,6,0,0"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Shader name" Margin="0,16,0,4"/>
        <TextBox x:Name="ShaderNameBox"
                 Grid.Row="2"
                 Padding="8,6"
                 SelectionBrush="{DynamicResource AccentDimBrush}"/>

        <TextBlock x:Name="ValidationText"
                   Grid.Row="3"
                   Foreground="{DynamicResource DangerBrush}"
                   TextWrapping="Wrap"
                   Margin="0,8,0,0"
                   Visibility="Collapsed"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource ChromeButtonStyle}" Margin="0,0,8,0"/>
            <Button Content="Use Shader name" IsDefault="True" Style="{StaticResource ChromeButtonStyle}" Click="AcceptButton_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
