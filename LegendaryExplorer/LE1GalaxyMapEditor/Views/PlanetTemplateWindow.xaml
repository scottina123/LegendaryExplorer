<Window x:Class="LE1GalaxyMapEditor.Views.PlanetTemplateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Planet template" Width="450" Height="330" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextBrush}">
    <Grid Margin="18">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Save the current visual parameters as a reusable template. The Shader name is intentionally excluded." TextWrapping="Wrap" Foreground="{DynamicResource MutedTextBrush}"/>
        <StackPanel Grid.Row="1" Margin="0,14,0,0">
            <TextBlock Text="Template name"/>
            <TextBox x:Name="NameBox" Padding="7,5" Margin="0,4,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Text="Description (optional)"/>
            <TextBox x:Name="DescriptionBox" Padding="7,5" Margin="0,4,0,0" Height="80"
                     VerticalContentAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" IsCancel="True" Padding="14,7" Margin="0,0,8,0"/>
            <Button Content="Save template" IsDefault="True" Padding="16,7" Click="Save_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
