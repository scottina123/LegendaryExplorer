<Window x:Class="LE1GalaxyMapEditor.Views.CommitPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Review changes"
        Width="680"
        Height="520"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}">
    <Border Margin="1"
            Padding="22"
            Background="{DynamicResource PanelBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="REVIEW CHANGES"
                       Foreground="{DynamicResource WarningBrush}"
                       FontSize="11"
                       FontWeight="Bold" />
            <TextBlock Grid.Row="1"
                       Margin="0,8,0,16"
                       Text="{Binding SummaryText}"
                       FontSize="15"
                       FontWeight="SemiBold" />

            <Border Grid.Row="2"
                    Background="{DynamicResource LE1GMEBrush0C151E}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1">
                <ScrollViewer x:Name="ChangeScrollViewer"
                              Padding="14"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Sections}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,18">
                                    <TextBlock FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap">
                                        <Run Text="{Binding ModuleDisplay}" />
                                        <Run Text="  •  " Foreground="{DynamicResource MutedTextBrush}" />
                                        <Run Text="{Binding FileName}" Foreground="{DynamicResource AccentBrush}" />
                                    </TextBlock>
                                    <ItemsControl ItemsSource="{Binding Entries}" Margin="0,8,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,7"
                                                        Padding="10,8"
                                                        Background="{DynamicResource PanelRaisedBrush}"
                                                        BorderBrush="{DynamicResource BorderBrush}"
                                                        BorderThickness="1">
                                                    <StackPanel>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding Title}"
                                                                       FontWeight="SemiBold"
                                                                       VerticalAlignment="Center" />
                                                            <Border Margin="8,0,0,0"
                                                                    Padding="5,1"
                                                                    Background="{DynamicResource AccentDimBrush}"
                                                                    BorderBrush="{DynamicResource AccentBrush}"
                                                                    BorderThickness="1"
                                                                    Visibility="{Binding HasBadge, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                <TextBlock Text="{Binding Badge}"
                                                                           Foreground="{DynamicResource AccentBrush}"
                                                                           FontSize="9"
                                                                           FontWeight="Bold" />
                                                            </Border>
                                                        </StackPanel>
                                                        <ItemsControl ItemsSource="{Binding Details}"
                                                                      Margin="14,5,0,0"
                                                                      Visibility="{Binding HasDetails, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <TextBlock Text="{Binding}"
                                                                               Margin="0,1"
                                                                               Foreground="{DynamicResource MutedTextBrush}"
                                                                               TextWrapping="Wrap" />
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="3"
                        Margin="0,18,0,0"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel"
                        IsCancel="True"
                        Click="CancelButton_OnClick"
                        Style="{StaticResource ChromeButtonStyle}"
                        Margin="0,0,8,0" />
                <Button x:Name="CommitButton"
                        Content="{Binding CommitButtonText}"
                        IsDefault="True"
                        Click="CommitButton_OnClick"
                        Style="{StaticResource ChromeButtonStyle}"
                        Background="{DynamicResource AccentDimBrush}"
                        BorderBrush="{DynamicResource AccentBrush}" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
